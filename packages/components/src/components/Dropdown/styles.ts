import { css } from 'lit'

export const styles = css`
    /* ThemePro 全局容器样式 */
    .kylinbits-container {
        pointer-events: none;
        z-index: 9999;
        position: fixed;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
    }
`
