import { css } from "../utils";
import * as vars from "../styles/vars.less?inline";
import * as icons from "../styles/icons.less?inline";
import * as utils from "../styles/utils.less?inline";
import * as light from "../themes/light.css?inline";

const themeproStyles = css`
    ${vars}
    ${icons}
    ${utils}
    ${light}
`;
